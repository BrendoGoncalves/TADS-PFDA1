#include <stdio.h>

int main(void){
	//Math operations
	int x = 2;
	int y = 4;
	int a, b, c ,d;

	a = x + y;
	b = x - y;
	c = (y / x) + 3;
	d = (x * y) * 2;
	
	printf("%i %i %i %i", a, b, c, d);
	
	return 0;
}
